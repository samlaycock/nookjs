# LogicalOperators

## What it is

Logical operators `&&`, `||`, and `??`.

## Interpreter implementation

- Implemented in `evaluateLogicalExpression` with short-circuit semantics.

## Gotchas

- Only `&&`, `||`, and `??` are supported.
