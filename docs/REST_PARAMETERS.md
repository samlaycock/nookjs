# RestParameters

## What it is

Collects extra arguments with `...rest`.

## Interpreter implementation

- Implemented in `bindFunctionParameters` / `bindFunctionParametersAsync`.
- Rest must be the last parameter.

## Gotchas

- No parameter destructuring with rest.
