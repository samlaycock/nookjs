# WhileStatement

## What it is

Looping with `while (condition) { ... }`.

## Interpreter implementation

- Implemented in `evaluateWhileStatement` / `evaluateWhileStatementAsync`.

## Gotchas

- Labeled `break`/`continue` are supported (see [LABELED_STATEMENT](LABELED_STATEMENT.md)).
